1. Query that shows IDs and durations of trips with duration greater than 500:

SELECT
    trip_id,
    duration
FROM
    trips
WHERE
    duration >= 500
ORDER BY duration DESC;

2.  Query that shows every column of the stations table for station id 84

SELECT
    *
FROM
    stations
WHERE
	station_id = '84'
	
3.  Query that shows the min temperatures of all the occurrences of rain in zip 94301

SELECT
    MinTemperatureF,
	Zip
FROM
    weather
WHERE
   	Zip = 94301
ORDER BY Zip DESC;
